# ── Microsoft SSO (Azure AD) ──────────────────────────────────────────────────
# Register an app at https://portal.azure.com → Azure Active Directory → App registrations
# Add redirect URI: https://your-domain.com/auth/callback (or http://localhost:3000/auth/callback for local)
#
# Leave these unset to use dev-login mode (no SSO, simple email/name form)
#
# Local-only SSO bypass (ignored when NODE_ENV=production). Same flags as Daily /
# Konsole / Meridien — auto-signs in without the form or Microsoft.
# BYPASS_SSO=true
# VITE_BYPASS_SSO=true
# VITE_FORCE_LOGIN_EMAIL=dev@local
# VITE_FORCE_LOGIN_NAME=Dev User

AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=woventalent.in
AUTH_REDIRECT_URI=http://localhost:3000/auth/callback

# ── Super Admin ───────────────────────────────────────────────────────────────
# The user with this email gets super_admin role (can create/manage all workspaces)
SUPER_ADMIN_EMAIL=

# ── Email (SMTP) ──────────────────────────────────────────────────────────────
# If unset, assignment emails are silently skipped
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@woventalent.in

# ── App URL ───────────────────────────────────────────────────────────────────
# Local: http://localhost:5173   Production: https://pulse.woventalent.in
APP_URL=http://localhost:5173

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development      # set to 'production' for secure cookies

# ── Konsole issues (must match Konsole KONSOLE_SERVICE_KEY / PULSE_ISSUES_KEY)
KONSOLE_SERVICE_KEY=
